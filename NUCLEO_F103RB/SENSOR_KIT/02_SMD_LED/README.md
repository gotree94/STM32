# SMD LED Module Test - NUCLEO-F103RB

SMD(Surface Mount Device) LED 모듈을 STM32F103 NUCLEO 보드에서 GPIO 및 PWM으로 제어하는 프로젝트입니다.

## 📌 개요

SMD LED 모듈은 표면실장형 LED로, 일반 스루홀 LED보다 밝고 소형이며 효율적입니다. 이 프로젝트에서는 GPIO를 이용한 단순 ON/OFF 제어와 PWM을 이용한 밝기 조절을 모두 테스트합니다.

## 🛠 하드웨어 구성

### 필요 부품
| 부품 | 수량 | 비고 |
|------|------|------|
| NUCLEO-F103RB | 1 | STM32F103RB 탑재 |
| SMD LED 모듈 | 1 | KY-011 또는 호환 모듈 |
| 점퍼 와이어 | 3 | Female-Female |

### 핀 연결

```
SMD LED Module          NUCLEO-F103RB
┌─────────────┐        ┌─────────────┐
│     S  ─────┼────────┤ PA5 (TIM2_CH1)
│   VCC  ─────┼────────┤ 3.3V
│   GND  ─────┼────────┤ GND
└─────────────┘        └─────────────┘
```

### 회로도

```
                3.3V
                 │
                 │
    ┌────────────┴────────────┐
    │       SMD LED Module    │
    │                         │
    │   ┌───┐                 │
    │   │LED│    R (내장)     │
    │   └─┬─┘    220Ω        │
    │     │                   │
    └─────┼───────────────────┘
          │
          S ──────── PA5
          │
         GND
```

> 💡 **참고**: 대부분의 SMD LED 모듈은 내장 저항이 있어 별도의 저항 없이 사용 가능합니다.

## 💻 소프트웨어

### 주요 기능

1. **GPIO 모드**: 단순 ON/OFF 점멸
2. **PWM 모드**: 0~100% 밝기 조절
3. **호흡 효과**: 점진적 밝기 변화
4. **SOS 신호**: 모스부호 패턴
5. **심박 효과**: 두 번 박동 패턴

### 동작 모드

```c
typedef enum {
    MODE_GPIO = 0,  // GPIO 디지털 출력
    MODE_PWM        // PWM 아날로그 출력
} LED_Mode_t;
```

### 주요 함수

```c
// 모드 전환
void LED_GPIO_Mode_Init(void);  // GPIO 모드로 전환
void LED_PWM_Mode_Init(void);   // PWM 모드로 전환

// LED 제어
void LED_SetBrightness(uint8_t brightness);  // 밝기 설정 (0~255)
void LED_Blink(uint32_t on_time, uint32_t off_time, uint8_t count);
void LED_Breathe(uint16_t cycle_ms);  // 호흡 효과
void LED_SOS(void);  // SOS 신호
```

### PWM 설정

```c
Timer: TIM2, Channel 1
Prescaler: 71 (72MHz / 72 = 1MHz)
Period: 999 (1MHz / 1000 = 1kHz PWM)
GPIO: PA5 (TIM2_CH1)
```

## 📂 프로젝트 구조

```
02_SMD_LED/
├── main.c          # 메인 소스 코드
└── README.md       # 프로젝트 설명서
```

## 🔧 빌드 및 실행

### STM32CubeIDE 사용 시
1. 새 STM32 프로젝트 생성 (NUCLEO-F103RB 선택)
2. `main.c` 내용을 프로젝트에 복사
3. 빌드 후 보드에 플래시

### Makefile 사용 시
```bash
make clean
make all
make flash
```

## 📊 시리얼 출력 예시

```
========================================
  SMD LED Module Test - NUCLEO-F103RB
========================================

[Test 1] GPIO Mode - Basic Blink
  Slow blink (500ms)...
  Fast blink (100ms)...
  Asymmetric blink (200/800ms)...

[Test 2] PWM Mode - Brightness Control
  Brightness levels: 0% 25% 50% 75% 100%

[Test 3] Breathing Effect
  2 second cycle...
  1 second cycle...

[Test 4] SOS Signal

[Test 5] Heartbeat Effect

--- Cycle Complete ---
```

## 📝 테스트 패턴 상세

### SOS 모스부호
```
S: · · ·  (짧게 3번)
O: — — —  (길게 3번)
S: · · ·  (짧게 3번)

타이밍:
- 점(dot): 150ms
- 선(dash): 450ms (점의 3배)
- 요소 간격: 150ms
- 문자 간격: 450ms
```

### 심박 효과
```
1. 첫 번째 박동: 0→255→0 (강하게)
2. 짧은 휴식: 100ms
3. 두 번째 박동: 0→180→0 (약하게)
4. 긴 휴식: 400ms
```

## 🔍 트러블슈팅

| 증상 | 원인 | 해결 방법 |
|------|------|----------|
| LED가 켜지지 않음 | 배선 오류 | VCC/GND 확인 |
| 밝기 조절 안됨 | 모드 설정 오류 | PWM 모드 확인 |
| 깜빡임이 느림 | PWM 주파수 낮음 | Period 값 조정 |
| LED가 너무 밝음 | 전류 과다 | 저항 추가 고려 |

## 💡 응용 예제

### 커스텀 점멸 패턴
```c
// 경고등 패턴 (빠른 이중 점멸)
void LED_Warning(void) {
    for (int i = 0; i < 5; i++) {
        LED_Blink(50, 50, 2);   // 빠르게 2번
        HAL_Delay(500);         // 긴 휴식
    }
}
```

### 레벨 인디케이터
```c
// 0~100 레벨을 LED 밝기로 표시
void LED_ShowLevel(uint8_t level) {
    if (level > 100) level = 100;
    LED_SetBrightness((level * 255) / 100);
}
```

## 📚 참고 자료

- [STM32F103 Reference Manual](https://www.st.com/resource/en/reference_manual/rm0008-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf)
- [KY-011 SMD LED Module](https://arduinomodules.info/ky-011-two-color-led-module-3mm/)

## 📜 라이선스

MIT License
