# Chapter 05: Runtime Environment (RTE)

[← 이전 챕터](../Chapter_04_Application_Software/README.md) | [메인으로 돌아가기](../README.md) | [다음 챕터 →](../Chapter_06_Communication_Stack/README.md)

---

## 📋 목차

1. [학습 목표](#1-학습-목표)
2. [RTE 개요](#2-rte-개요)
3. [RTE API](#3-rte-api)
4. [RTE 통신 메커니즘](#4-rte-통신-메커니즘)
5. [RTE 생성 과정](#5-rte-생성-과정)
6. [요약 및 연습 문제](#6-요약-및-연습-문제)

---

## 1. 학습 목표

```
✅ RTE의 역할과 VFB와의 관계 이해
✅ RTE API 종류와 사용법 학습
✅ Intra-ECU / Inter-ECU 통신 메커니즘 이해
✅ RTE 생성 과정 파악
```

---

## 2. RTE 개요

### 2.1 RTE란?

**RTE(Runtime Environment)** 는 VFB의 실제 구현체로, Application Layer와 BSW 사이의 통신을 담당합니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                        RTE 역할                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Application Layer (SWC)                     │   │
│  │   ┌─────────┐    ┌─────────┐    ┌─────────┐            │   │
│  │   │  SWC A  │    │  SWC B  │    │  SWC C  │            │   │
│  │   └────┬────┘    └────┬────┘    └────┬────┘            │   │
│  └────────┼──────────────┼──────────────┼──────────────────┘   │
│           │              │              │                       │
│           │   RTE API    │   RTE API    │                       │
│           ▼              ▼              ▼                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                        RTE                               │   │
│  │                                                          │   │
│  │   주요 기능:                                             │   │
│  │   • SWC ↔ SWC 통신 (Intra-ECU)                          │   │
│  │   • SWC ↔ BSW 통신                                      │   │
│  │   • Inter-ECU 통신 추상화                               │   │
│  │   • Runnable 스케줄링 지원                              │   │
│  │                                                          │   │
│  └───────────────────────────┬──────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                        BSW                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 RTE 특징

| 특징 | 설명 |
|------|------|
| **자동 생성** | RTE Generator에 의해 자동 생성 |
| **ECU 종속** | ECU 설정에 따라 맞춤 생성 |
| **표준 API** | SWC에 표준화된 통신 API 제공 |
| **정적 구성** | 컴파일 타임에 모든 연결 결정 |

---

## 3. RTE API

### 3.1 RTE API 분류

| API 종류 | 용도 | 예시 |
|----------|------|------|
| **Rte_Read** | S/R 데이터 읽기 | `Rte_Read_RP_Speed_value(&speed)` |
| **Rte_Write** | S/R 데이터 쓰기 | `Rte_Write_PP_Torque_value(torque)` |
| **Rte_Send/Receive** | Queued S/R | `Rte_Receive_RP_Msg_data(&msg)` |
| **Rte_Call** | C/S 호출 | `Rte_Call_RP_Diag_ReadData(...)` |
| **Rte_IrvRead/Write** | IRV 접근 | `Rte_IrvRead_Swc_FilteredVal()` |
| **Rte_Mode** | 모드 읽기 | `Rte_Mode_RP_EcuMode_mode()` |

### 3.2 Sender-Receiver API 예시

```c
/* ═══════════════════════════════════════════════════════════════
 * Non-Queued (Last-is-Best) 통신
 * ═══════════════════════════════════════════════════════════════ */

void RE_Control(void)
{
    uint16 speed;
    float32 torque;
    
    /* R-Port에서 읽기 */
    (void)Rte_Read_RP_Speed_value(&speed);
    
    /* 계산 */
    torque = CalculateTorque(speed);
    
    /* P-Port로 쓰기 */
    (void)Rte_Write_PP_Torque_value(torque);
}

/* ═══════════════════════════════════════════════════════════════
 * Queued (FIFO) 통신
 * ═══════════════════════════════════════════════════════════════ */

void RE_ProcessMessage(void)
{
    MessageType msg;
    Std_ReturnType status;
    
    /* 큐에서 메시지 수신 */
    status = Rte_Receive_RP_Message_data(&msg);
    
    if (status == RTE_E_OK) {
        ProcessMessage(&msg);
    }
}
```

### 3.3 Client-Server API 예시

```c
/* ═══════════════════════════════════════════════════════════════
 * 동기 호출 (Synchronous)
 * ═══════════════════════════════════════════════════════════════ */

void RE_DiagRequest(void)
{
    uint8 buffer[64];
    uint16 length;
    Std_ReturnType result;
    
    /* 동기 호출 - 완료까지 대기 */
    result = Rte_Call_RP_DiagService_ReadData(0x1234, buffer, &length);
    
    if (result == RTE_E_OK) {
        /* 성공 처리 */
    }
}

/* Server 측 구현 */
FUNC(Std_ReturnType, SWC_CODE) DiagService_ReadData(
    uint16 dataId,
    uint8* data,
    uint16* length
)
{
    if (dataId == 0x1234) {
        data[0] = GetValue1();
        *length = 1;
        return E_OK;
    }
    return E_NOT_OK;
}
```

### 3.4 Inter-Runnable Variable (IRV)

```c
/* 동일 SWC 내 Runnable 간 데이터 공유 */

void RE_FilterSpeed(void)  /* 10ms Runnable */
{
    uint16 rawSpeed;
    float32 filteredSpeed;
    
    Rte_Read_RP_Speed_value(&rawSpeed);
    filteredSpeed = ApplyFilter((float32)rawSpeed);
    
    /* IRV에 저장 */
    Rte_IrvWrite_Swc_FilteredSpeed(filteredSpeed);
}

void RE_CalculateTorque(void)  /* 20ms Runnable */
{
    float32 speed;
    
    /* 다른 Runnable이 계산한 값 읽기 */
    speed = Rte_IrvRead_Swc_FilteredSpeed();
    
    /* 토크 계산에 사용 */
}
```

---

## 4. RTE 통신 메커니즘

### 4.1 Intra-ECU 통신 (동일 ECU 내)

```
     SWC A                                    SWC B
    ┌─────────┐                            ┌─────────┐
    │ [P] Out │                            │ [R] In  │
    └────┬────┘                            └────┬────┘
         │ Rte_Write()                          │ Rte_Read()
         ▼                                      ▼
    ┌─────────────────────────────────────────────────┐
    │                     RTE                          │
    │         ┌─────────────────────────┐             │
    │         │   Internal Buffer (RAM) │             │
    │         │                         │             │
    │         │  Write → [Buffer] → Read│             │
    │         └─────────────────────────┘             │
    │                                                  │
    │   * 직접 메모리 복사, BSW 거치지 않음, 매우 빠름 │
    └─────────────────────────────────────────────────┘
```

### 4.2 Inter-ECU 통신 (다른 ECU)

```
     ECU 1                                    ECU 2
  ┌────────────────┐                    ┌────────────────┐
  │  SWC A         │                    │         SWC B  │
  │  └─► RTE       │                    │       RTE ◄─┘  │
  │      └─► COM   │                    │   COM ◄─┘      │
  │          └─► CanIf                  │   CanIf ◄─┘    │
  │              └─► Can                │   Can ◄─┘      │
  └────────────────┼────────────────────┼────────────────┘
                   └───────► CAN Bus ◄──┘
                   
  * COM에서 Signal → CAN Message 패킹
  * 네트워크를 통해 전송
```

### 4.3 통신 Semantics 비교

| 특성 | Last-is-Best | Queued |
|------|--------------|--------|
| 저장 방식 | 최신값만 저장 | FIFO 큐에 모두 저장 |
| 용도 | 센서값, 상태값 | 이벤트, 명령 |
| API | Rte_Read/Write | Rte_Receive/Send |

---

## 5. RTE 생성 과정

### 5.1 RTE Generator

```
   입력 (ARXML)                              출력 (Generated Code)
  ┌─────────────────┐                      ┌─────────────────────┐
  │ SWC Description │                      │ Rte.c / Rte.h       │
  │ System Config   │───► RTE Generator ───│ Rte_<SWC>.h         │
  │ ECU Config      │                      │ Rte_Type.h          │
  └─────────────────┘                      └─────────────────────┘
```

### 5.2 2-Phase 생성

| Phase | 입력 | 출력 | 목적 |
|-------|------|------|------|
| **Contract** | SWC Description | Rte_<SWC>.h | SWC 독립 개발 |
| **Implementation** | 전체 Config | Rte.c | 실제 코드 생성 |

### 5.3 생성된 RTE 코드 예시

```c
/* Rte.c (Generated) */

/* 내부 버퍼 */
static uint16 Rte_Buf_SWC_A_PP_Speed_value;

/* Write API */
Std_ReturnType Rte_Write_SWC_A_PP_Speed_value(uint16 data)
{
    Rte_Buf_SWC_A_PP_Speed_value = data;
    return RTE_E_OK;
}

/* Read API */
Std_ReturnType Rte_Read_SWC_B_RP_Speed_value(uint16* data)
{
    *data = Rte_Buf_SWC_A_PP_Speed_value;
    return RTE_E_OK;
}
```

---

## 6. 요약 및 연습 문제

### 핵심 정리

```
┌─────────────────────────────────────────────────────────────────┐
│                    Chapter 05 핵심 정리                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📌 RTE 역할:                                                    │
│     ├── VFB의 실제 구현체                                       │
│     ├── SWC ↔ SWC 통신 (Intra-ECU)                             │
│     ├── SWC ↔ BSW 통신                                         │
│     └── Inter-ECU 통신 추상화                                   │
│                                                                 │
│  📌 RTE API 종류:                                                │
│     ├── Rte_Read/Write: S/R 통신                               │
│     ├── Rte_Call: C/S 통신                                     │
│     └── Rte_IrvRead/Write: IRV 접근                            │
│                                                                 │
│  📌 통신 Semantics:                                              │
│     ├── Last-is-Best: 최신 값만 유지                           │
│     └── Queued: FIFO 큐 사용                                   │
│                                                                 │
│  📌 RTE 2-Phase 생성:                                            │
│     ├── Contract Phase: API 헤더만 생성                        │
│     └── Implementation Phase: 실제 코드 생성                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 연습 문제

**문제 1.** Intra-ECU 통신과 Inter-ECU 통신의 차이점은?

<details>
<summary>정답</summary>

- **Intra-ECU**: 동일 ECU 내 SWC 간 통신. RTE 내부 버퍼로 직접 메모리 복사. 매우 빠름.
- **Inter-ECU**: 다른 ECU와 통신. COM, CanIf, Can 등 BSW 통신 스택을 거쳐 네트워크로 전송.
</details>

**문제 2.** Last-is-Best와 Queued 통신의 사용 사례는?

<details>
<summary>정답</summary>

- **Last-is-Best**: 센서값, 상태값 (최신 값만 필요)
- **Queued**: 이벤트, 명령 (모든 값 처리 필요)
</details>

**문제 3.** RTE Contract Phase의 목적은?

<details>
<summary>정답</summary>

SWC 코드 컴파일을 위한 헤더(Rte_<SWC>.h)를 생성하여, 시스템 통합 전에 SWC를 독립적으로 개발/컴파일 가능하게 합니다.
</details>

---

[← 이전 챕터](../Chapter_04_Application_Software/README.md) | [메인으로 돌아가기](../README.md) | [다음 챕터 →](../Chapter_06_Communication_Stack/README.md)
